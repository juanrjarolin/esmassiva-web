# =============================================================================
# Archivo de configuración de entorno para esmassiva
# Copia este contenido a un archivo .env en la raíz del proyecto
# =============================================================================

# Entorno de ejecución
NODE_ENV=development

# URL base de la aplicación
BASE_URL=http://localhost:3000
# BASE_URL_OTHER_PORT=http://localhost:8000

# Contraseña de administrador
ADMIN_PASSWORD=tu_password_seguro

# Base de datos MySQL (ajusta según tu instalación local)
DATABASE_URL=mysql://root:tu_password@localhost:3306/esmassiva

# MinIO (almacenamiento de archivos)
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=tu_password_seguro
MINIO_BUCKET=esmassiva
MINIO_USE_SSL=false

