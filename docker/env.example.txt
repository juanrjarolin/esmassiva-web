# =============================================================================
# Variables de entorno para Docker
# Copia este archivo como .env en la carpeta docker/
# =============================================================================

# Entorno (development o production)
NODE_ENV=development

# MySQL
MYSQL_ROOT_PASSWORD=root
MYSQL_PASSWORD=esmassiva

# Contraseña de administrador (para MinIO y htpasswd)
ADMIN_PASSWORD=admin123

# Opcional: IP de escucha (por defecto 127.0.0.1, usa 0.0.0.0 para acceso externo)
# LISTEN_IP=0.0.0.0

# URL base de la aplicación (importante para producción)
# En desarrollo, puede dejarse vacío o usar http://localhost:8000
# En producción, usar la URL completa: https://tudominio.com
# BASE_URL=http://localhost:8000

# Ruta base de la aplicación (ej: /esmassiva-web)
# Si la aplicación se sirve desde una subcarpeta, configurar aquí
# Debe empezar con / y terminar sin /
# BASE_PATH=/esmassiva-web

